# ActivityWork (Next.js app)

Project documentation, architecture, and local service URLs are in the **[repository root README](../README.md)**.

From this directory:

```bash
npm install
npm run dev
```

The app listens on [http://localhost:5601](http://localhost:5601) (`npm run dev` / `npm run start`).
